# 构建产物
bin/
obj/
release/

# 运行时生成（含用户蓝牙地址等个人配置，绝不入库）
config.json
config.json.tmp
*.log
inject.log

# 调试符号（tap.dll 的预编译产物入库，pdb 不需要）
*.pdb

# 开发临时文件
_tray_preview.png
*.user

# 本地 AI 辅助开发说明，不入库
CLAUDE.md
